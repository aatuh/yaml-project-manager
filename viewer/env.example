########################################
# Docker Compose host-side settings
########################################

# Absolute path on HOST that contains your data repo/root.
# This directory should include either:
#  - workspace-data/ (default layout)
#  - or data/projects/{projects.yaml,projects/,seasons/} (DATA_DIR layout)
HOST_DATA_DIR=/home/user/project-control-map/workspace-data

# Map container port to host. Keep 3000 unless you need another.
PORT=3000

# Your host user/group ids so files created by the container are owned by you.
# Get values via: id -u  and  id -g
UID=1000
GID=1000

########################################
# App runtime settings
# Used by Next.js when running on host (pnpm dev/start)
########################################

# Optional: override data root when running on host.
# If set, the app reads from:
#   $DATA_DIR/data/projects/{projects.yaml,projects/,seasons/}
# If unset, it reads ../workspace-data relative to the app.
# Example:
# DATA_DIR=/home/user/project-control-map

# Auto-commit writes (git) inside the data repo. 1 = enabled, 0 = disabled.
AUTO_COMMIT=0

# Dev file watching (useful on some filesystems). Normally not needed on host.
CHOKIDAR_USEPOLLING=1
WATCHPACK_POLLING=true
